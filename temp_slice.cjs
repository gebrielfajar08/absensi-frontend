const fs=require('fs'); const s=fs.readFileSync('src/pages/Landing.jsx','utf8'); const lines=s.split(/\r?\n/); const start=2108-1, end=2125; for(let i=start;i<end;i++){ console.log((i+1)+': '+ lines[i]); } console.log('---chars---'); const slice=lines.slice(start,end).join('\n'); console.log(JSON.stringify(slice)); console.log('counts:', {braces_open:(slice.match(/\{/g)||[]).length, braces_close:(slice.match(/\}/g)||[]).length, backticks:(slice.match(/`/g)||[]).length});
